// App build number. BUMP this on every deploy that should prompt an update,
// and set the same number in the DB (battlechis_config.app_version) so clients
// on an older build show the "Actualizar" button.
export const APP_VERSION = 1;
